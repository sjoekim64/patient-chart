[Patient Chart System - USB 사용 안내]

1) 실행 (개발 모드)
   - 'start-app.cmd' 더블클릭
   - 처음 실행이면 자동으로 필요한 파일을 설치합니다.
   - 설치 후 브라우저에서 http://localhost:5173 로 열립니다.

2) 실행 (배포 버전 미리보기)
   - 'build-and-preview.cmd' 더블클릭
   - 빌드 후 미리보기 서버가 열립니다.

3) 로그인 정보 (테스트)
   - 사용자명: sjoekim
   - 비밀번호: Joe007007
   - 메인 화면 우상단 '관리자 페이지' 버튼으로 이동/복귀 가능

4) 참고 사항
   - 인터넷이 가능하면 처음 한 번만 설치됩니다.
   - 개인 환경 변수(.env.local)는 PC별로 설정하세요(선택).
   - 데이터는 브라우저에 저장되므로 PC/브라우저별로 따로 관리됩니다.

5) 문제 해결
   - Node.js가 없으면 설치 안내가 표시됩니다(18 이상 권장).
   - 문제가 있으면 폴더에서 'npm ci' 후 다시 실행해보세요.
